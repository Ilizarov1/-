/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function (s, p) {
    if (p == null) return false;

    var exp = Array.from(p);
    var str = Array.from(s);
    for (let i = 0; i < exp.length; i++){
       
    }
};
console.log(isMatch("a", "ab"));